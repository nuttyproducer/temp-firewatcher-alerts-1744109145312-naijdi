
// This file re-exports toast functions from sonner for use throughout the app
import { toast as sonnerToast } from "sonner";

// Re-export the toast function for use in components
export const toast = sonnerToast;
